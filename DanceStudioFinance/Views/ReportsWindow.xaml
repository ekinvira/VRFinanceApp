<Window x:Class="VRFinanceApp.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Raporlar"
        Height="760"
        Width="1100"
        MinHeight="700"
        MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F3F6FB">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="White"
                    CornerRadius="18"
                    Padding="24"
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Raporlar"
                                   FontSize="30"
                                   FontWeight="Bold"
                                   Foreground="#1F2937"/>
                        <TextBlock Text="Aylık filtre ile gelir ve gider analizleri"
                                   Margin="0,6,0,0"
                                   FontSize="14"
                                   Foreground="#6B7280"/>
                    </StackPanel>

                    <Border Grid.Column="1"
                            Background="#EEF2FF"
                            CornerRadius="14"
                            Padding="16,10">
                        <StackPanel>
                            <TextBlock Text="Genel Bakış"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="#4F46E5"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Reports"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Foreground="#312E81"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Filters -->
            <Border Grid.Row="1"
                    Background="White"
                    CornerRadius="18"
                    Padding="24"
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Ay"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#374151"
                                   Margin="0,0,0,6"/>
                        <ComboBox x:Name="cmbMonth"
                                  Width="180"
                                  Height="38"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,16,0">
                        <TextBlock Text="Yıl"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#374151"
                                   Margin="0,0,0,6"/>
                        <ComboBox x:Name="cmbYear"
                                  Width="120"
                                  Height="38"/>
                    </StackPanel>

                    <Button Grid.Column="2"
                            x:Name="btnLoadReport"
                            Content="Raporu Getir"
                            Width="160"
                            Height="40"
                            Margin="0,24,0,0"
                            Background="#2563EB"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="btnLoadReport_Click"/>
                </Grid>
            </Border>

            <!-- Summary Cards -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="#22C55E"
                        CornerRadius="18"
                        Padding="20"
                        Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Aylık Toplam Gelir"
                                   FontSize="14"
                                   Foreground="White"
                                   Opacity="0.9"/>
                        <TextBlock x:Name="txtMonthlyIncome"
                                   Text="0 ₺"
                                   Margin="0,10,0,0"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1"
                        Background="#EF4444"
                        CornerRadius="18"
                        Padding="20"
                        Margin="6,0">
                    <StackPanel>
                        <TextBlock Text="Aylık Toplam Gider"
                                   FontSize="14"
                                   Foreground="White"
                                   Opacity="0.9"/>
                        <TextBlock x:Name="txtMonthlyExpense"
                                   Text="0 ₺"
                                   Margin="0,10,0,0"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </StackPanel>
                </Border>

                <Border x:Name="borderMonthlyNet"
                        Grid.Column="2"
                        Background="#3B82F6"
                        CornerRadius="18"
                        Padding="20"
                        Margin="12,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Aylık Net Sonuç"
                                   FontSize="14"
                                   Foreground="White"
                                   Opacity="0.9"/>
                        <TextBlock x:Name="txtMonthlyNet"
                                   Text="0 ₺"
                                   Margin="0,10,0,0"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Income Report -->
            <Border Grid.Row="3"
                    Background="White"
                    CornerRadius="18"
                    Padding="24"
                    Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Branşa Göre Gelir"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="#1F2937"
                               Margin="0,0,0,14"/>

                    <DataGrid x:Name="dgIncomeReport"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              RowBackground="White"
                              AlternatingRowBackground="#F8FAFC"
                              BorderThickness="0"
                              MinHeight="220"
                              MaxHeight="260"
                              RowHeight="38"
                              ColumnHeaderHeight="40">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Branş"
                                                Binding="{Binding Branch}"
                                                Width="*"/>
                            <DataGridTextColumn Header="Toplam Gelir"
                                                Binding="{Binding TotalAmount}"
                                                Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Expense Report -->
            <Border Grid.Row="4"
                    Background="White"
                    CornerRadius="18"
                    Padding="24">
                <StackPanel>
                    <TextBlock Text="Kategoriye Göre Gider"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="#1F2937"
                               Margin="0,0,0,14"/>

                    <DataGrid x:Name="dgExpenseReport"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              RowBackground="White"
                              AlternatingRowBackground="#F8FAFC"
                              BorderThickness="0"
                              MinHeight="220"
                              MaxHeight="260"
                              RowHeight="38"
                              ColumnHeaderHeight="40">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Kategori"
                                                Binding="{Binding Category}"
                                                Width="*"/>
                            <DataGridTextColumn Header="Toplam Gider"
                                                Binding="{Binding TotalAmount}"
                                                Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Window>